name: forge-test

on:
  push:
    branches: [main, "feature/**", "fix/**"]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
          fetch-depth: 0

      - name: Install Foundry
        uses: foundry-rs/foundry-toolchain@v1
        with:
          # Pinned to the stable release the suite is validated against (716/716).
          # `nightly` introduced stricter cheatcode/gas behavior that breaks
          # test harnesses without any production-code change — do not float this.
          version: "v1.7.1"

      - name: forge --version
        run: forge --version

      - name: forge build
        run: forge build

      - name: forge test
        run: forge test --no-match-path "test/fork*" -vv

      - name: forge coverage (informational)
        run: forge coverage --no-match-path "test/fork*" --report summary || true
        continue-on-error: true

